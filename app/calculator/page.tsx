'use client';
import { useMemo,useState } from 'react';
export default function CalculatorPage(){const [price,setPrice]=useState(1500000);const [down,setDown]=useState(20);const [other,setOther]=useState(0);const result=useMemo(()=>{const d=price*down/100;return{down:d,balance:price-d,total:price+other}},[price,down,other]);return <><section className="section"><div className="container"><span className="eyebrow">Planning tool</span><h1 className="display">Simple purchase budget worksheet.</h1><p className="lede">Enter figures from a current written quotation. This tool does not estimate returns, mortgage approval or official transaction fees.</p></div></section><section className="section section-soft"><div className="container contact-grid"><div className="lead-form"><label className="field-label">Property price (AED)<input className="field-input" type="number" min="1" value={price} onChange={e=>setPrice(Number(e.target.value))}/></label><label className="field-label">Initial payment (%)<input className="field-input" type="number" min="0" max="100" value={down} onChange={e=>setDown(Number(e.target.value))}/></label><label className="field-label">Other confirmed costs (AED)<input className="field-input" type="number" min="0" value={other} onChange={e=>setOther(Number(e.target.value))}/></label><p className="fine-print">Add only costs confirmed for your transaction, such as registration, trustee/service-partner, mortgage, valuation or conveyancing charges.</p></div><div><span className="eyebrow">Worksheet result</span><h2 className="section-title">AED {result.total.toLocaleString()}</h2><div className="facts"><div className="fact"><span>Initial payment</span><strong>AED {result.down.toLocaleString()}</strong></div><div className="fact"><span>Remaining price</span><strong>AED {result.balance.toLocaleString()}</strong></div><div className="fact"><span>Other entered costs</span><strong>AED {other.toLocaleString()}</strong></div><div className="fact"><span>Entered total</span><strong>AED {result.total.toLocaleString()}</strong></div></div><p className="fine-print">Illustrative arithmetic only. It excludes costs you have not entered and does not constitute a quotation or financial advice.</p></div></div></section></>}
