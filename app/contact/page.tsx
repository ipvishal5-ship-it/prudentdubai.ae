import type { Metadata } from 'next';
import { Suspense } from 'react';
import { getSiteSettings } from '@/lib/content';
import LeadForm from './LeadForm';
export const metadata:Metadata={title:'Contact',description:'Share your Dubai property requirements with the Prudent Dubai team.'};
export default async function ContactPage(){const site=await getSiteSettings();return <><section className="section"><div className="container"><span className="eyebrow">Talk to Prudent</span><h1 className="display">Begin with your requirements.</h1><p className="lede">Tell us whether you are looking for a home, an investment comparison or a specific project. We will not treat a broad enquiry as permission to send unrelated promotions.</p></div></section><section className="section section-soft"><div className="container contact-grid"><aside className="contact-list"><div className="contact-item"><span>Phone / WhatsApp</span><strong>{site.phone}</strong><div className="button-row"><a className="button button-primary" href={`https://wa.me/${site.whatsapp}`} target="_blank" rel="noopener noreferrer">Open WhatsApp</a></div></div><div className="contact-item"><span>Email</span><a href={`mailto:${site.email}`}>{site.email}</a></div><div className="contact-item"><span>Dubai office</span><p>{site.dubaiOffice}</p></div><div className="contact-item"><span>Privacy</span><p className="fine-print">Please do not send passport copies, bank statements, card information or identity documents through this public form.</p></div></aside><Suspense fallback={<div className="lead-form"><p>Loading form…</p></div>}><LeadForm whatsapp={site.whatsapp} /></Suspense></div></section></>}
